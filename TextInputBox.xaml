<UserControl x:Class="WpfControlsLib.TextInputBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfControlsLib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Label, RelativeSource={RelativeSource FindAncestor,AncestorType=UserControl},Mode=OneTime}"
                   Margin="0,0,5,0"></TextBlock>
        <TextBox Grid.Column="1" Text="{Binding Text, RelativeSource={RelativeSource FindAncestor,AncestorType=UserControl},Mode=TwoWay}" 
                 MinWidth="10"></TextBox>
            
    </Grid>
</UserControl>
